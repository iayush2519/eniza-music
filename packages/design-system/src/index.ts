/**
 * @music-app/design-system
 *
 * Design tokens and themed primitive components, built in Phase 2
 * (Design system foundations). Left empty in Phase 1 — this package exists
 * now so the workspace dependency graph and build pipeline are correct
 * before any real UI work starts.
 */

export const DESIGN_SYSTEM_PACKAGE_NAME = '@music-app/design-system' as const;
