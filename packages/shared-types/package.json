{
  "name": "@music-app/shared-types",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Shared TypeScript types/DTOs used by both the mobile app and the backend API.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@music-app/config": "workspace:*",
    "eslint": "10.7.0",
    "rimraf": "6.0.1",
    "typescript": "5.9.3"
  }
}
